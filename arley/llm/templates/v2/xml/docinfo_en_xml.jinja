The following contains an XML-document (enclosed in XML-tags "xmldata").
It contains an original request (XML-tag "original_prompt"), your original response to that (XML-tag "original_response") and further information and examples for that (enclosed in XML-tag "context").
Within the XML-tag "context", there may be:
  - Exemplary outlines (each one enclosed in XML-tags "contract_outline")
  - Exemplary summaries (each one enclosed in XML-tags "contract_summary")
  - Exemplary phrases (each one enclosed in XML-tags "contract_excerpt")
  - Exemplary contracts (each one enclosed in XML-Tags "contract").