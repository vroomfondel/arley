{%  if refine_type == "contract_outline" %}Exemplary outlines (each one enclosed in xml-tags "{{ refine_type }}"):{% set sinfo = "Exemplary outline" %}{% endif %}
{%  if refine_type == "contract_summary" %}Exemplary outlines (each one enclosed in xml-tags "{{ refine_type }}"){% set sinfo = "Exemplary summary" %}{% endif %}
{%  if refine_type == "contract_excerpt" %}Exemplary phrases (each one enclosed in xml-tags "{{ refine_type }}"):{% set sinfo = "Exemplary excerpt" %}{% endif %}
{%  if refine_type == "contract" %}Exemplary contracts (each one enclosed in xml-tags "{{ refine_type }}"):{% set sinfo = "Exemplary contract" %}{% endif %}

{% for meaug in merefine %}
<{{ refine_type }}>
{{ meaug["document"].strip() }}
</{{ refine_type }}>
{% endfor %}